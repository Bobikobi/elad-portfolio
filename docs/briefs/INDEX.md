# Brief / verify audit trail

One line per stage. A stage is not done until its verify artifact says PASS against the
deployed preview alias.

| stage | date | verdict | commit |
|---|---|---|---|
| [B8b](B8b-brief.md) - project windows as annular sectors ([verify](B8b-verify.md)) | 2026-08-03 | PASS - all 7 criteria on the preview alias, criterion 6 on a real GPU at both tiers | f11f3aa (+ harness follow-up) |
| [F2](F2-brief.md) - classic view toggle ([verify](F2-verify.md)) | 2026-08-03 | PASS - all 5 criteria on the preview alias; SSR asserted off the wire, 30/30 | 0d9150f |
| [B8c](B8c-verify.md) - owner corrections to the projects ring (no brief: five specified fixes) | 2026-08-03 | PASS - 3 windows, damped scroll, no inner box, whole window clickable, scroll rail; caught and fixed a 42fps mobile regression this round introduced | c25a13e |
| [B8d](B8d-brief.md) - previews in the ring plane, words on the planet ([verify](B8d-verify.md)) | 2026-08-03 | PARTIAL - redesign + navbar + header done and measured; 60fps fails at low tier (53.5-55.5) | ab1441f (lane `feat/b8c`) |
| scene mount ruling - canvas only on cosmic routes (verify inside B8d-verify) | 2026-08-03 | PASS - 20/20 route x mode combinations; service detail 660KB/391ms TBT -> 235KB/0ms | ab1441f (lane `feat/b8c`) |
| [SCENE-FLICKER](SCENE-FLICKER-verify.md) - twinkle / bloom / aliasing / occlusion / chromatic aberration | 2026-08-03 | PARTIAL - candidate 5 disproven with numbers, 1 and 3 real and fixed at one shared line, 2 untested, 4 no evidence; the artefact itself was not reproduced at 450ms sampling | 36a99fb (lane `feat/b8c`) |
| [STATUS 2026-08-04](STATUS-2026-08-04.md) - scene lane, everything since the B8d brief | 2026-08-04 | open items: merge PR #26 and #27, retarget PR #24, logos, reviewer capture conditions, knip deletions | 4584fae (lane `feat/b8c`) |
| [PROJECTS-HOVER](PROJECTS-HOVER-brief.md) - pointer input reaches the windows, and the two-stage tap ([verify](PROJECTS-HOVER-verify.md)) | 2026-08-05 | PASS - 9 criteria on the preview alias, against production as the control; found and fixed a live defect where the scroll container swallowed every pointer event, so hover showed nothing and clicking a project did nothing (7/7 cases). Criterion 1's "12/12 windows" is unmeetable: 1 window is hit-testable in portrait, 2 at 1440x900 | ea956a4 + 4218278 (lane `feat/projects-hover-touch`, PR #31) |
| [SUN-2](SUN-2-brief.md) - granulation, limb darkening, live limb, deeper lanes ([verify](SUN-2-verify.md)) | 2026-08-08 | **C2's BAND IS SUPERSEDED (owner ruling 2026-09-10): the 0.80-0.88 range is retired in favour of P7's S4, limb/centre <= 0.75.** The band was set against a previous build with no photograph in the room; a real solar photograph measures centre-to-limb at 1.79, i.e. a limb at 0.559 of the centre - darker than either criterion allowed. Measured after P7: 0.743. C3 and C4 PASS, C5 pass with its reference corrected. C1 fails the number (2.16x of 2.5x) and passes the eye - the threshold was a guess and the crops are unambiguous. ~~C2 unreachable from the shader: 48% of limb darkening arrives as 8%, bloom fills it back in~~ **CORRECTED 2026-08-15 by SUN-3: C2 is reachable and was measured at 0.804. The limb was being filled back in by GOD RAYS, not bloom - bloom moved the ratio by 0.001.** The R2.2 halo was re-measured and got tighter, not wider | ec0c4cc (lane `feat/sun-2`) |
| [home-return](home-return-verify.md) - a visitor returning from a world can scroll back (PR #27) | 2026-08-08 | PASS on PRODUCTION - 3/3 cases: document 4500 vs 900, parked at 3600/3600, 0 of ~530 frames spent animating, galaxy reachable | bbd4261 (merged) |
| [projects upright](projects-upright-verify.md) - the previews were rendered upside down | 2026-08-08 | PASS - a marker image showed 225deg at 1440x900 and 275deg at 390x844; upright and unmirrored at both after the fix, tap harness still 5/5. First diagnosis (a mirror) was wrong and is recorded as such | a2c2067 (lane `feat/sun-2`) |
| recorded defect, no stage yet - **Mars's own focused world clips 5.0% of its disc above 250**, and did so before SUN-3. Not caused by it. The `ORBIT_EXPOSURE` calibration for that world has aged out. Recorded 2026-08-15 because it had never been written down anywhere; the fix is inherited by P4 of [the photometry plan](PHOTOMETRY-megaplan.md) | 2026-08-15 | OPEN - measured on a real GPU, localhost. **CORRECTED 2026-09-15: 5.0% was one longitude. Mars keeps spinning in its own world; over a full turn the shipped scene clips its disc from 0.00% to 11.10% (worst at 297.9 deg), red channel only. Owner decision the same day: this stays a separate defect and P4 is not required to fix it - P4 may only not make it worse. Measured with `mars-turn`, see [P4-brief.md](P4-brief.md). Lead found during P4: the rolloff can nearly remove it - strength 1.0 on a steeper curve took the worst longitude to 0.74% - at the cost of about a third of Mars's red** | 0fcf349 (lane `feat/sun-2`) |
| recorded defect, no stage yet - **Venus burns to white on the overview**, and `highlightRolloff` cannot fix it | 2026-09-15 | OPEN - a separate item by owner decision. 1,483 of her 2,081 overview disc pixels clip in all three channels. P4 measured that the rolloff itself makes part of that white (held off, 434 px), but her highlights are as saturated as Mars's, so in eleven candidates every setting that helped Venus made Mars burn more. Needs a Venus-specific lever - her albedo multiplier (2.76) or her exposure, which is P3's territory rather than the rolloff's. See [P4-brief.md](P4-brief.md) | (lane `mobile-1-wip`) |
| [SUN-3](SUN-3-brief.md) - the sun's red channel was a plateau ([verify](SUN-3-verify.md), [plan](PHOTOMETRY-megaplan.md)) | 2026-08-15 | C1/C2/C3/C5 PASS on localhost, real GPU - limb/centre red 1.000 -> 0.873, luminance 0.920 -> 0.804 monotone, longest prominence 1.76R -> 1.09R. NOT yet measured on a preview alias (rule 2). C4 (planet clipping) FAILS and is deliberately out of scope - it is albedo, not the sun. Corrects SUN-2's C2 verdict: the limb was filled back in by GOD RAYS, not bloom. **Brief and verify written after the fact on 2026-08-15 and labelled as such (P0a).** ~~The verify is UNSIGNED~~ **CLOSED 2026-09-14: all three blockers cleared.** Re-measured on a deployed alias against localhost the same hour - C1 0.841/0.840, C2 0.757/0.755, C3 1.11R/1.10R, C5 6px/7px, every delta inside the live-clock noise, so the recorded worry that a dev-vs-production build difference could hide in the tone chain is answered. Route 2 turned out already implemented (`foreign_circles`, 4 objects excluded). Route 3 was written (`sun-3-route3.py`) and passes - at one ring per pixel the longest non-falling run is 1.78% of R against a 3% bar, so C2's monotonicity is not an artifact of coarse binning. CAVEAT: P3, P4 and P7 all moved the sun after SUN-3, so these describe TODAY's sun, not `0fcf349`'s. C2's 0.757 fails a band RULING 4 retired | 0fcf349 (lane `feat/sun-2`) |
| [P3](P3-verify.md) - albedo and falloff: eight real albedos, softened falloff under RULING 3, five apertures recalibrated ([plan](PHOTOMETRY-megaplan.md)) | 2026-08-17 | 4 of 5 PASS on localhost, real GPU - no body disappears (neptune 51->101), the worlds hold within +/-8, the tier law unchanged, Venus still brightest. P3-1 is 4 of 6 views at zero; Earth's cloud tops and Venus remain and are proven NOT fixable by P4 - they are neutral white and highlightRolloff is gated by chroma spread. Two criteria were corrected: P3-1's threshold counted brightness at 235 rather than damage at 250, and P3-3's monotonicity clause contradicted RULING 3. ~~NOT signed under rule 2~~ **SIGNED 2026-09-14** - re-reported from the `alias-p3` deployed capture, all five criterion lines identical to localhost number for number | (lane `codex/p1-pilot`) |
| [P7](P7-brief.md) - richness: what the sun should LOOK like ([verify](P7-verify.md), [plan](PHOTOMETRY-megaplan.md)) | 2026-08-24 | **PASS - all six criteria, and SIGNED on a deployed alias 2026-09-14** where every number came back identical to localhost to every decimal, fitted disc radius included. S1 granulation, S2 hot core, S3 corona 2.95x sky, S4 limb/centre 0.7425 under RULING 4's <= 0.75, S5 granulation lifetime, S6 tier law. The alias capture (`alias1`) existed from 2026-09-10 and the verify was never updated to say so; corrected. **This row did not exist until 2026-09-14** - P7 was complete and verified with no line in the audit trail | (lane `codex/p1-pilot`) |
| [P0](P0-verify.md) - correct the audit trail, and close SUN-3's paperwork ([plan](PHOTOMETRY-megaplan.md)) | 2026-09-14 | **COMPLETE - all six acceptance items done**, the last of them today. No code, no pixels. P0a: the SUN-3 brief and verify exist and the brief is labelled after-the-fact AND names its sources, stating "nothing here is recalled from conversation"; the alias re-measurement landed 2026-09-14 with every delta inside the live-clock noise, answering the plan's stated worry that a dev-vs-production difference could hide in the tone chain. P0b: all three wrong conclusions corrected IN SITU with dated notes rather than deleted. Records a FOURTH wrong conclusion the plan's own list did not know about - `0fcf349` moved the overview bloom threshold 0.94 -> 0.33 silently and left the comment explaining 0.94 in place; corrected, value deliberately untouched because it is P6's second knob. **This stage's subject is the audit trail and it was itself missing from it** | (lane `codex/p1-pilot`) |
| [P1](P1-verify.md) - the photometric module: one file owns the scene's light budget ([plan](PHOTOMETRY-megaplan.md)) | 2026-09-14 | **PASS, and the verify is written four weeks after the fact - labelled as such.** P1-1 exactly zero on all six views, measured retroactively over `11ac5f9..24b936c`: the instrument that makes it exact was BUILT INSIDE P1, so the pre-P1 tree was made measurable by adding only the clock seam (3 lines + DebugHud) with `toneMappingExposure` left at the literal 1. P1-2 calls and triangles identical at both tiers, medians +0.1/+0.7ms, via a new `p1-cost.mjs` that reads `window.__hud`, which predates P1. **P1-3 is unmeetable as written** - it forbids naming `toneMappingExposure`, which forbids using it - corrected to zero NUMERIC LITERALS, which passes. Added a check the plan lacked: all 16 constants compared statically against their pre-P1 literals, 16 of 16 identical, because a six-view pixel diff cannot see a constant that only matters elsewhere. Found and fixed a latent harness bug: `visibility` is discrete and the header's `transition: all 0.3s` delayed it past `hideDom`'s 400ms wait - luck on every earlier run. NOT signed under rule 2 - localhost only | 24b936c (lane `codex/p1-pilot`) |
| [P2](P2-verify.md) - measure every body's peak ([plan](PHOTOMETRY-megaplan.md)) | 2026-09-14 | **PASS and SIGNED under rule 2** - all eight bodies with peak linear radiance, real GPU, from the deployed alias, which is all four conditions the plan asked for. Written after the fact and labelled. Records a ranking P3's own account does not: **Saturn blows 51.45% of its disc on the overview**, against Jupiter 12.85% and Mars 11.24%, while Venus - the body P3 names - blows 0.00% and peaks at 0.6867 linear. The two instruments count different things (all-three-channels over 250 vs each body's own peak channel) and rank the bodies differently. P4 should know that before it picks a target. Neptune peaks at 0.0953, the only body with real headroom | (lane `codex/p1-pilot`) |
| [P5](P5-verify.md) - close the holes in the criteria ([plan](PHOTOMETRY-megaplan.md)) | 2026-09-14 | **DONE - both items.** Harness and docs only, no pixels. P5.1: the annulus at 1.1-2.0R already existed as P7's S3 and the connection was never recorded; the half that was missing is the one that matters. New `sky_radial_profile()` takes the sky's median in bands from the FRAME centre with the sun and corona excluded, and **demonstrates the blindness rather than arguing it - R2.2's corner blocks read 0.000% on a frame whose middle sits at 8.211%, a factor of 24**. It deliberately does NOT invert the vignette's own constants to find its reach, because that would be a threshold derived from the source under audit - which is exactly what P5.2 forbids, and P5.1 may not break it in the same commit. P7's six criteria unchanged with it in place. P5.2: now **standing rule 6**, with two additions to the plan's wording - that the failure runs in BOTH directions (SUN-3's threshold reported a real improvement as a failure; the same mechanism reports a real failure as progress), and that a constant in the audited source is not a legitimate source either. Not signed under rule 2 - it adds no criterion that judges the scene | (lane `mobile-1-wip`) |
| [P4](P4-brief.md) - re-scope `highlightRolloff` ([plan](PHOTOMETRY-megaplan.md)) | 2026-09-14 | **CLOSED 2026-09-15 - MEASURED, NOT SHIPPED (owner decision).** Eleven candidates: every setting that gives visible colour back harms Mars or Venus, and the only setting that harms nothing (0.73) is indistinguishable by eye from today. No shipped pixel moved; the validated ?hl/?noRolloff seams and the new harnesses are kept. History of the stage follows. ~~CRITERIA PROPOSED, NOT APPROVED - no pixel moved.~~ Corrects two numbers the plan stands on. **Mars is at 0.08%, not 5.0%** - P3 already took it there, and zero pixels clip in all three channels - so P4 inherits a guard and a 0.03pp gap, not a fix. And **the acceptance has no instrument**: nothing in this repo measures chroma on a planet disc, so the criterion cannot be judged until one is built and validated on unchanged code. Raises a body neither P3's nor P4's account mentions - **Saturn blows 51.45% of its disc on the overview, four times the next worst**, and whether it is P4's at all depends on its channel spread, which nobody has measured. Puts three questions to the owner, the first being whether P4 is still worth its risk now that it inherits 0.03pp rather than 5.0% - the real prize is quality (the sun loses 49% of its core saturation to this function), not a defect fix. **STEP 1 RESULT 2026-09-14, VALID - a second capture of the same build reads exactly zero against the first - and it overturns three things in this brief:** the sun loses 0.35% at its core, not 49%, so the prize this brief named does not exist on today's sun; Saturn IS in reach (97% of its disc, 48% of its chroma taken); and with the rolloff ON, Venus clips in all three channels 1,483 px against 434 OFF - the function manufactures the neutral burnt white the plan recorded as untouchable, because that neutrality was measured AFTER the function had already run. **FULL TURN 2026-09-15: Mars clips up to 11.10% of its disc at its worst longitude with the rolloff on (44.83% off, 0.00% at 7 of 24 longitudes) - the 0.08%, 0.29% and 1.87% were single longitudes of a planet that keeps spinning in its own world, so P4 inherits a real failing criterion, not a 0.03pp gap.** New `mars-turn` harness; shot 1 byte-identical to p3-albedo's capture at the same frame. P4-1 restated as a paired full-turn comparison. **Owner decided 2026-09-15: P4-1 is that rule, Mars 11.10% stays a separate defect. Criteria revised against measurements, each goal set halfway to the function-off frame (Saturn chroma >= 0.0928, Venus burnt white <= 958 px, Earth's <= 8,749), APPROVED by the owner 2026-09-15.** **SCREENING: the ?hl candidate seam validated (absent = byte-identical, today's values reproduce p4-on exactly). Round 1, both candidates FAIL - every setting that gave colour back made Mars worse (gentlest: 11.10% -> 15.99%), and Venus did not respond to a steeper curve at all (1,483 -> 1,468), consistent with her highlights being as saturated as Mars's. Round 2: three candidates hold Mars and nearly fix its defect (11.10% -> 0.74%), but take a third of Mars's colour, burn Venus more (-> ~1,600) and dim Neptune. In all five comparable candidates Venus and Mars moved against each other - the Venus goal and the Mars guard conflict inside this function. Earth passes every time; Saturn's halfway target reached by none. Round 3 (the balance point) running. A capture race found - wait and freeze sent as two browser calls, so a frame could slip in between - and audited: every Mars turn and every view capture so far is aligned, no result affected. Round 3: along one curve every quantity trades one way with strength; Mars's guard crosses at ~0.73, and the only window where Mars holds and nothing loses colour is narrow (0.73-0.74, being measured) with a modest outcome - Earth's target met, Saturn about +13% and short of halfway, Venus unchanged. No candidate in three rounds reached the Venus or Saturn targets; revising them is the owner's call. Window measured with the race fixed and validated (a no-?hl turn identical to turn-on on all 25 shots): at strength 0.73 Mars holds (10.97%), Earth's burnt white -12.9%, Saturn +13.6% colour, and the only loss anywhere is Venus - 15 more burnt pixels and 6% of an already near-grey colour. No setting holds Mars with Venus untouched; the modest version needs Venus out of P4 entirely. At 0.73 the sun's S1-S6 all pass essentially identical to today and the tier law is unchanged, so with Venus out it passes every measured criterion; only the owner's look remains. P4-8 EVIDENCE: galaxy captured before/after and a side-by-side sheet sent to the owner. 0.73 changes up to 23% of a frame's pixels but by about ONE level of 255 on average (galaxy hotspot mean 1.01, Earth 0.95); at full resolution with the difference amplified, before and after are indistinguishable by eye. Safe, and too small to see - whether to ship it is the owner's call. No shipped pixel moved** | (lane `mobile-1-wip`) |
| [MOBILE-1](MOBILE-1-brief.md) - stop the scene blocking the main thread at startup ([verify](MOBILE-1-verify.md)) | 2026-09-11 | **PARTIAL - M3 and M4 PASS, M1 and M2 FAIL, and the eye finds a regression the numbers miss.** M3 was failing on the overview alone (mean 2.115, max 248) and is fixed: the new bitmap loader's `dispose()` was terminal, React's useMemo/useEffect pairing let a cleanup land on the live shell, and every planet was rendering with a null albedo map - Saturn's globe black inside intact rings. The five worlds passed only because `uHiMix` covered the black base map. All six views now 0.0000/0. M4 identical to P7. M1 10,114ms against a 5,000 target and a 10,188 baseline - **no movement**; M2 1,600ms against 600. Total main-thread time got WORSE, 20,341 -> 30,946ms: the work was sliced into more tasks, not reduced. By eye, the loader now lifts before the galaxy exists. Also settles the TBT definition the brief's numbers came from, which was not recorded anywhere. **SIGNED under rule 2 on 2026-09-14**: M3 re-run alias-to-alias at 0.0000/0, M1/M2 on the aliases at 10,797/1,664 - same verdict. The localhost dev capture is BYTE-IDENTICAL to the alias capture on all six views, so the photometry harness can be iterated locally and rule 2 costs one confirming run. Fixed a harness bug in `mobile-startup.mjs` that made any vercel.app run crash. **2026-09-14, owner approved: the frame-spreading is REVERTED** (StartupReveal deleted, Warmup back to frame 3, the galaxy back to one useMemo) and only the off-main-thread decode is kept. The galaxy reveal comes back and is EARLIER than the baseline, 5,603ms against 7,571ms. Found one more regression inside the loader itself - `gl.initTexture` on every base map put all three runs of the longest task above all three baseline runs; pre-upload is now opt-in and only `loadHiRes` keeps it. Shipped branch: M1 10,374 against a same-day baseline of 10,391, M2 1,793 against 1,773 - **moving the decode off the main thread bought nothing measurable**, which is what the re-brief starts from | (lane `mobile-1-wip`) |
| [MOBILE-2](MOBILE-2-brief.md) - the startup cost, aimed at where it actually is ([N0 verify](MOBILE-2-N0-verify.md)) | 2026-09-14 | **N0 PASS - unattributed 25.16% -> 0.00%, nothing donated.** The residual diagnostic proved 100% of the hole was covered by both CPU samples and trace events with 0.0ms genuinely dark; 93.6% was V8's `(program)` sentinel, which the report was dropping while the trace already held a name for it. Retroactive: `prod20` and `live20`, captured before the change, also come back 0.00%. The three hottest entry points resolved from the built chunks - React's scheduler 3,171ms, R3F's frame loop 2,310ms, and a SECOND React scheduler inside the R3F chunk 1,851ms. New synchronous-GPU-query phase names 306ms, and is opt-in because it costs 30% of the thread it measures (an estimate of ~300ms was wrong by twenty times and is corrected in place). **N0 found no new lever**, so the M2 target decision stands where the brief left it. Remaining criteria PROPOSED, NOT APPROVED - no product code yet.** Follows MOBILE-1, which did not close. Carries the attribution MOBILE-1 never had: the longest task (1,642ms) is one animation frame holding `gl.compile`, and the cost inside it is **107 synchronous program-status queries plus 46 GPU command-buffer waits, ~426ms** - not the compile, which is 27ms. The second task (1,598ms) is bundle evaluation and is not the scene. DOM layout and paint are 2,184ms, ~11%, also not the scene. **A quarter of the main thread (5,059ms) is unattributed**, so N0 closes that hole BEFORE any lever is chosen. Adds N5 - the scene must be whole at the frame the loader lifts - because MOBILE-1 passed four numbers while the hero frame lost its galaxy. Flags that M2's 600ms may be unreachable in scope and puts three options to the owner. **OWNER DIRECTION 2026-09-14: phones get a NEW view, designed after the current work - gallery-like, one planet at a time, swipe to the next, no free camera.** Recorded in the brief as a direction with its open questions, not designed | (lane `mobile-1-wip`) |
